trigger:
  - main

variables:
  nodeVersion: '20.x'
  npmCacheDir: '$(Pipeline.Workspace)/.npm'

stages:
  - stage: BuildAndTest
    displayName: Playwright Excel Tests
    jobs:
      - job: RunExcelTests
        displayName: Run Playwright Excel Tests
        pool:
          vmImage: 'ubuntu-latest'

        steps:
          - task: NodeTool@0
            inputs:
              versionSpec: '$(nodeVersion)'
            displayName: 'Install Node.js'

          - script: |
              mkdir -p $(npmCacheDir)
              npm config set cache $(npmCacheDir)
            displayName: 'Configure npm cache'

          - task: Cache@2
            inputs:
              key: 'npm | "$(Agent.OS)" | package-lock.json'
              restoreKeys: |
                npm | "$(Agent.OS)"
              path: '$(npmCacheDir)'
            displayName: 'Cache npm modules'

          - script: |
              npm ci
            displayName: 'Install dependencies'

          # Install Playwright browser
          - script: |
              npx playwright install chromium
              sudo npx playwright install-deps chromium
            displayName: 'Install Playwright Chromium'

          # Run tests and generate JUnit + HTML
          - script: |
              npx playwright test \
                --reporter=junit,html \
                --output=test-results
            displayName: 'Run Playwright tests'

          # Surface results in Tests tab (from Microsoft doc)
          - task: PublishTestResults@2
            displayName: 'Publish results to Tests tab'
            inputs:
              testResultsFormat: 'JUnit'
              testResultsFiles: 'test-results/*.xml'
              searchFolder: '$(System.DefaultWorkingDirectory)'
              mergeTestResults: true
              testRunTitle: 'Playwright UI automation tests'
              failTaskOnFailedTests: false

          # Publish detailed HTML report (with steps)
          - task: PublishPipelineArtifact@1
            inputs:
              targetPath: 'playwright-report'
              artifact: 'playwright-html-report'
            displayName: 'Publish Playwright HTML report'
